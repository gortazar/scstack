# Gerrit Service

description     "Gerrit daemon"

start on runlevel [2345]
stop on starting rc RUNLEVEL=[016]

respawn
respawn limit 2 5

env GERRIT_SITE=<%=installFolder%>/gerrit
umask 007

kill timeout 300

exec <%=installFolder%>/gerrit/bin/gerrit.sh run

